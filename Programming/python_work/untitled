def damage(damage, speed, time):
    if damage or speed < 1:
        return("invalid")
    else:
        if time == "seconds":
            return damage * speed
        elif time == "minutes":
            return damage * speed * 60
        elif time == "hours":
            return damage * speed * 3600

print(damage(10, 2, "seconds"))